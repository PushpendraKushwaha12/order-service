server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservices_db
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always

jwt:
  secret: mySecretKeyForJWTTokenGenerationAndValidationPurposeOnlyExtended123456
  expiration: 86400000